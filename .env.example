# Server Configuration
PORT=5000

# Database Configuration (PostgreSQL / Neon)
CONNECTION_STR="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require&channel_binding=require"

# JWT Configuration
JWT_SECRET=your-jwt-secret-at-least-256-bits-long
